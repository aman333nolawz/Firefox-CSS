# Firefox-CSS
Dracula themed userchrome.css for firefox

# Setup
1. Goto `about:profiles` in firefox and click on `Open Directory` of `Root Directory` in your profile.
2. Clone this repo:
```bash
git clone https://github.com/aman333nolawz/Firefox-CSS.git chrome
```
3. Restart firefox!
